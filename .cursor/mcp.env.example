# Para que serve: secrets dos servidores MCP do Cursor (template).
# Função: modelo para copiar → .cursor/mcp.env local — nunca commitar tokens reais.
# Copy to .cursor/mcp.env (gitignored) — never commit real tokens
# GitHub PAT: scope `repo` (add `read:org` if needed)
GITHUB_PERSONAL_ACCESS_TOKEN=ghp_REPLACE_WITH_EXAMPLE_TOKEN_ONLY
